This project implements a Car Counting System using Python that detects and counts the number of cars in a pre-recorded video. It uses computer vision techniques powered by OpenCV and optionally a pre-trained object detection model YOLO .

The system processes each frame of the video, detects cars, and keeps track of the number of cars passing a certain point or within a specific region of interest.
